// not allowed to reply, so each post is about a student's fun fact to share.
thread = {
    _id: ObjectId,
    date: Date,
    author: String,
    title: String,
    content: String,
    reply: xxx
}
